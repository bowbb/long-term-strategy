services:
  strategy-web:
    image: ghcr.io/bowbb/long-term-strategy:1.0.1
    container_name: long-term-strategy
    ports:
      - "${PORT:-8000}:8000"
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
