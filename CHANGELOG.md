# 更新日志

## Unreleased

### 策略与计算

- 明确区分实际持仓比例与策略状态；有效策略状态只有基础仓位 100% 和基础仓位 50%，不再接受 `0.0` 作为滞回状态。
- 刷新时全量下载并替换本地历史，手动计算只读取刷新后的本地历史并扫描最近一次从高到低跌破下轨或从低到高突破上轨的信号。
- USD/CNY 或美元资产底层数据刷新失败时，纳斯达克100和黄金的人民币派生序列整体沿用上一份，不混合新旧依赖数据。

## v1.3.0 - 2026-08-18

### 策略

- 风险资产跌破 `MA250 × 0.97` 后降至基础仓位的 50%，不再清空；价格回到滞回区间时保持 50% 或 100% 当前状态。
- 没有历史状态时，区间内按 100% 基础仓位初始化，低于下轨按 50% 初始化；新增资金在滞回区间内仍按当前目标比例投入。
- 增加半仓状态的持久化识别，避免半仓在下一次计算时被误恢复为满仓。

### 数据

- 红利低波改用中证指数官方 `H20269` 全收益指数，分红已嵌入指数点位；境内长期国债继续使用上交所 `511260`，Nasdaq `QQQ`、State Street `GLD` 和美联储 H.10 `DEXCHUS` 保持官方数据口径。
- 官方刷新整段替换本地历史，记录来源 URL、检查时间、最新值、行数和序列 SHA-256；旧代理/比例缩放数据迁移到隔离目录，不再参与计算。
- 纳斯达克和黄金的人民币合成只保留 ETF 实际交易日，汇率在这些日期内前向填充。

### 回测与测试

- 增加 H20269 2006 年以来、2016 年以来的全卖与卖至基础仓位 50%回测结果。
- 增加 H20269 解析、半仓状态保持和新策略状态文案测试。
- 默认 Docker Compose 镜像更新为 `ghcr.io/bowbb/long-term-strategy:1.3.0`。

## v1.2.0 - 2026-08-12

### 策略

- 信号改为使用最新已完成交易日数据，并在下一个交易日执行，不再限定月末。
- MA250 ±3% 滞回区间内按当前输入持仓延续：已有仓位暂时不动，没有仓位暂时不买。
- 从项目中彻底移除已不再配置的两类零权重资产；运行资产精简为红利低波、纳斯达克100、黄金、境内长期国债和人民币现金。
- 中国交易日历改用红利低波行情，最新数据概览仅显示四类有价格的资产。

### 工程

- 升级时自动清理已移除资产的行情缓存、旧持仓键、旧计算快照和旧刷新日志条目。
- 增加每日信号、持仓滞回、资产清理与用户文案测试，共 11 项单元测试。
- 默认 Docker Compose 镜像更新为 `ghcr.io/bowbb/long-term-strategy:1.2.0`。

## v1.1.1 - 2026-08-12

### 修复

- 目标金额与当前金额相差不超过 0.01 元、不需要实际下单时，预估佣金不再错误计入单笔最低 0.3 元。
- 默认 Docker Compose 镜像更新为 `ghcr.io/bowbb/long-term-strategy:1.1.1`。

## v1.1.0 - 2026-08-12

### 策略

- 正式基础权重更新为红利低波 30%、纳斯达克100 50%、黄金 10%、境内长期国债 5%、人民币现金 5%。
- 删除原 MA20/MA250 两分制与 50% 中间仓位，改为 MA250 ±3% 有状态滞回。
- 每个完整月末产生一次信号，并在下一个中国交易日执行；滞回区间内维持上月状态。
- 风险资产释放资金各 50% 转入境内长期国债和人民币现金；历史不足 250 个交易日的基础权重全部转入长期国债。
- 交易计划按 ETF 每笔万 0.6、最低 0.3 元估算佣金。

### 工程

- 页面、配置和说明彻底移除旧策略字段，并展示 MA250 上下轨、正式信号日和执行日。
- 增加滞回边界、月末信号、缺失历史与佣金计算的单元测试。
- 默认 Docker Compose 镜像更新为 `ghcr.io/bowbb/long-term-strategy:1.1.0`。

## v1.0.2 - 2026-08-12

### 修复

- 删除现金收益代理序列，不再把人民币现金显示为需要更新的行情资产。
- 最新数据概览只显示需要行情的策略资产，不显示人民币现金。
- 现金仍保留为手动输入金额、5%基础仓位和调仓去向，不参与价格或均线计算。
- 启动时自动清理旧版本留下的 `data/prices/cash.csv`。
- 默认 Docker Compose 镜像更新为 `ghcr.io/bowbb/long-term-strategy:1.0.2`。

## v1.0.1 - 2026-08-12

### 修复

- USD/CNY 不再依赖 FRED 单一远程接口。
- 增加 Yahoo Finance `CNY=X`、ECB 官方历史参考汇率、Frankfurter/ECB 和 FRED 的顺序回退。
- 任一远程汇率源成功时，记录实际数据源并将纳斯达克100、黄金标记为成功；只有使用本地缓存时才显示警告。

### 工程

- 增加不依赖外网的汇率解析和人民币合成单元测试。
- GitHub Actions 在构建镜像前执行单元测试。
- 默认 Docker Compose 镜像更新为 `ghcr.io/bowbb/long-term-strategy:1.0.1`。
