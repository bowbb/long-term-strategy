services:
  strategy-web:
    build: .
    image: long-term-strategy:local
    container_name: long-term-strategy
    ports:
      - "${PORT:-8000}:8000"
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
