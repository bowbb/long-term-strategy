# 种子数据

`seed/prices/` 保留历史研究快照，仅供人工比对，不再由网页自动复制到 `data/prices/`。网页只接受通过官方数据源整段刷新并通过质量检查的四类行情；人民币现金由网页手动输入，不使用价格序列。

容器运行后，网页刷新只更新 `data/` 挂载目录中的本地文件；旧版本文件会保留在 `data/legacy_prices/`，不会参与计算。`data/` 目录还保存设置、来源审计日志、输入的持仓和最新计算结果。
